version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: country-db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: countrydb
    ports:
      - "5432:5432"